# 2026-07-23 工作记录

## 将项目推送到新建的 GitHub 仓库
- 项目：猫咪社区 Android App（包名 com.leaf.collegeidleapp，校园闲置/猫咪社区，纯本地 SQLite，无后端；Gradle 3.6.1 + SDK 28）。
- 新建仓库：**MOnnn199/campus-idle-market**（公开，默认分支 master），已推送全部代码。
- 提供的 GitHub Token 实际属于账号 **MOnnn199**（不是旧远程 yunju-ustc/catshequ 那个账号），新仓库建在 MOnnn199 下。
- 旧远程 `origin`（yunju-ustc/catshequ）已改名为 `catshequ-old` 保留备份；新仓库设为 `origin`。
- 写了 README.md（中文，含功能、技术栈、运行方式、结构、数据存储），并强化 .gitignore（Android 标准），清理了误跟踪的 .idea 等。
- 安全：Token 仅用于建仓库+推送，未写入任何文件；`.git/config` 中已无 Token 残留（用一次性 insteadOf 注入 + 关闭 sslVerify 绕过本机证书吊销问题）。
- 已知限制：本机沙箱无法落盘 `.git/refs/remotes/origin/master`，`git status` 显示 `master...origin/master [gone]`；但远程仓库正常。以后推送用 `git push origin master`（需凭据）。
